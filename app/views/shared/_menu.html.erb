<nav class="navbar navbar-default navbar-fixed-top" role="navigation" style="margin-bottom: 0">
    <div class="navbar-header">
      </br>
      <a class="navbar-brand" href="#">Trupe Money</a>
    </div>
    <!-- /.navbar-header -->

    <ul class="nav navbar-top-links navbar-right">
        <% unless user_logged? %>
            <li class="divider"></li>
            <%= link_to("/auth/facebook", :class => "btn btn-block btn-social btn-facebook") do %>
                 <i class="fa fa-facebook"></i> Sign in with Facebook
           <% end %>
            <li class="divider"></li>
        <% else %>
            <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                    <%= image_tag current_user.photo_url if user_logged? %></i> <%= current_user.name %> <i class="fa fa-caret-down"></i> 
                </a>
                <ul class="dropdown-menu dropdown-user">
                    <li><a href="#"><i class="fa fa-user fa-fw"></i> User Profile</a>
                    </li>
                    <li class="divider"></li>
                    <li><a href="signout"><i class="fa fa-sign-out fa-fw"></i> Logout</a>
                    </li>
                </ul>
                <!-- /.dropdown-user -->
            </li>
        <% end %>
        
    </ul>
    <!-- /.navbar-top-links -->
</nav>

